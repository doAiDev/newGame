# Flutter web deploy disabled - replaced by Unity WebGL (deploy-webgl.yml)
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Flutter web deploy is disabled"
